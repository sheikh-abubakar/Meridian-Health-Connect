import { ExternalLink } from "lucide-react";
import { openPdfPreview } from "@/api/client";

const stamp = (value) => new Intl.DateTimeFormat("en-PK", { day: "numeric", month: "short", year: "numeric", hour: "numeric", minute: "2-digit", hour12: true, timeZone: "Asia/Karachi" }).format(new Date(value));
export function PortalPrescriptionsSection({ prescriptions, accessToken }) {
  return <><h1 className="text-3xl font-bold">My prescriptions</h1><p className="mt-2 text-slate-600">Prescriptions issued by your clinician after completed visits.</p><div className="mt-6 space-y-4">{prescriptions.length ? prescriptions.map((prescription) => <article key={prescription.id} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex flex-wrap justify-between gap-3"><div><h2 className="text-xl font-bold">{prescription.visitType}</h2><p className="mt-1 text-sm text-slate-500">Issued by {prescription.doctor} · {stamp(prescription.issuedAt)}</p></div><button onClick={() => openPdfPreview(`/patient-portal/prescriptions/${prescription.id}/pdf`, accessToken)} className="flex min-h-10 items-center gap-2 rounded-lg border px-3 font-semibold text-teal-800"><ExternalLink className="size-4" />Open PDF</button></div><div className="mt-4 space-y-3">{prescription.items.map((item) => <div key={item.id} className="rounded-xl bg-slate-50 p-4"><p className="font-bold">{item.medicineName} · {item.strength}</p><p className="mt-1 text-sm text-slate-700">{item.frequency} · {item.duration}</p>{item.instructions && <p className="mt-2 text-sm text-slate-600">{item.instructions}</p>}</div>)}</div></article>) : <p className="rounded-2xl border border-dashed bg-white p-7 text-center text-slate-500">No prescriptions have been issued yet.</p>}</div></>;
}
