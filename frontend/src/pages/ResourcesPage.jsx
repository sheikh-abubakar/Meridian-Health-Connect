import { useEffect, useMemo, useState } from "react";
import { Boxes, CheckCircle2, ClipboardList, Plus, Settings2, Stethoscope } from "lucide-react";
import { useParams } from "react-router-dom";
import { apiRequest } from "@/api/client";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { useAuth } from "@/context/auth-context";
import { useRealtimeRevision } from "@/realtime/useRealtimeRevision";

const newVisit = () => ({ name: "", durationMinutes: "20", specialtyIds: [], requiredResourceType: "none" });
function Choices({ items, selected, onChange }) { return <div className="grid gap-2 rounded-lg border p-3 sm:grid-cols-2">{items.map((item) => <label className="flex items-center gap-2 text-sm" key={item.id}><input className="size-4 accent-teal-700" type="checkbox" checked={selected.includes(item.id)} onChange={(e) => onChange(e.target.checked ? [...selected, item.id] : selected.filter((id) => id !== item.id))} />{item.name}</label>)}</div>; }

export function ResourcesPage() {
  const { tenantSlug, locationSlug } = useParams(); const { session } = useAuth(); const root = `/${tenantSlug}/${locationSlug}`;
  const headers = useMemo(() => ({ Authorization: `Bearer ${session.accessToken}` }), [session.accessToken]);
  const revision = useRealtimeRevision(["resource:created", "resource:updated", "specialty:created", "visittype:created"]);
  const [resources, setResources] = useState([]); const [specialties, setSpecialties] = useState([]); const [visitTypes, setVisitTypes] = useState([]);
  const [settings, setSettings] = useState({ maxOverbookSlotsPerDoctorPerDay: 0, reminderHours: 12 });
  const [resource, setResource] = useState({ name: "", type: "room" }); const [specialtyName, setSpecialtyName] = useState(""); const [visit, setVisit] = useState(newVisit());
  const [resourceOpen, setResourceOpen] = useState(false); const [specialtyOpen, setSpecialtyOpen] = useState(false); const [visitOpen, setVisitOpen] = useState(false); const [notice, setNotice] = useState(""); const [error, setError] = useState("");
  useEffect(() => { Promise.all([apiRequest(`${root}/resources`, { headers }), apiRequest(`${root}/resources/specialties`, { headers }), apiRequest(`${root}/resources/visit-types`, { headers }), apiRequest(`${root}/context`, { headers })]).then(([r, s, v, c]) => { setResources(r.resources); setSpecialties(s.specialties); setVisitTypes(v.visitTypes); const setting = c.location.schedulingSettings || {}; setSettings({ maxOverbookSlotsPerDoctorPerDay: setting.maxOverbookSlotsPerDoctorPerDay || 0, reminderHours: setting.reminderRules?.find((rule) => rule.channel === "sms")?.offsetHours ?? 12 }); }).catch((e) => setError(e.message)); }, [headers, revision, root]);
  async function createResource(e) { e.preventDefault(); try { const data = await apiRequest(`${root}/resources`, { method: "POST", headers, body: JSON.stringify(resource) }); setResources((list) => [...list, data.resource]); setResourceOpen(false); setResource({ name: "", type: "room" }); setNotice(`${data.resource.name} is now available during booking.`); } catch (err) { setError(err.message); } }
  async function createSpecialty(e) { e.preventDefault(); try { const data = await apiRequest(`${root}/resources/specialties`, { method: "POST", headers, body: JSON.stringify({ name: specialtyName }) }); setSpecialties((list) => [...list, data.specialty].sort((a, b) => a.name.localeCompare(b.name))); setSpecialtyOpen(false); setSpecialtyName(""); setNotice(`${data.specialty.name} is now available for Doctor and visit-type configuration.`); } catch (err) { setError(err.message); } }
  async function createVisitType(e) { e.preventDefault(); try { const data = await apiRequest(`${root}/resources/visit-types`, { method: "POST", headers, body: JSON.stringify({ ...visit, durationMinutes: Number(visit.durationMinutes), requiredResourceType: visit.requiredResourceType === "none" ? null : visit.requiredResourceType }) }); setVisitTypes((list) => [...list, data.visitType]); setVisitOpen(false); setVisit(newVisit()); setNotice(`${data.visitType.name} is now available for Front-desk booking.`); } catch (err) { setError(err.message); } }
  async function saveSettings(e) { e.preventDefault(); try { await apiRequest(`${root}/resources/settings`, { method: "PATCH", headers, body: JSON.stringify({ maxOverbookSlotsPerDoctorPerDay: Number(settings.maxOverbookSlotsPerDoctorPerDay), reminderRules: [{ channel: "sms", offsetHours: Number(settings.reminderHours) }] }) }); setNotice("Scheduling safeguards and SMS reminder timing were saved for this branch."); } catch (err) { setError(err.message); } }
  return <><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-teal-700">Clinic administration</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Scheduling setup</h1><p className="mt-2 text-muted-foreground">Configure provider specialties, visit templates, resources and branch scheduling safeguards.</p></div><div className="flex flex-wrap gap-2">
    <Dialog open={specialtyOpen} onOpenChange={setSpecialtyOpen}><DialogTrigger asChild><Button variant="outline"><Stethoscope className="mr-2 size-4" />Add specialty</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>Add provider specialty</DialogTitle></DialogHeader><form className="space-y-4" onSubmit={createSpecialty}><Input value={specialtyName} onChange={(e) => setSpecialtyName(e.target.value)} placeholder="e.g. Cardiology" required /><DialogFooter><Button className="bg-teal-700 hover:bg-teal-800">Save specialty</Button></DialogFooter></form></DialogContent></Dialog>
    <Dialog open={visitOpen} onOpenChange={setVisitOpen}><DialogTrigger asChild><Button className="bg-teal-700 hover:bg-teal-800"><Plus className="mr-2 size-4" />Add visit type</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>Add visit type</DialogTitle></DialogHeader><form className="space-y-4" onSubmit={createVisitType}><div><Label>Name</Label><Input className="mt-2" value={visit.name} onChange={(e) => setVisit({ ...visit, name: e.target.value })} required /></div><div><Label>Duration (minutes)</Label><Input className="mt-2" type="number" min="5" max="480" value={visit.durationMinutes} onChange={(e) => setVisit({ ...visit, durationMinutes: e.target.value })} required /></div><div><Label>Eligible provider specialties</Label><div className="mt-2"><Choices items={specialties} selected={visit.specialtyIds} onChange={(specialtyIds) => setVisit({ ...visit, specialtyIds })} /></div></div><div><Label>Required resource</Label><Select value={visit.requiredResourceType} onValueChange={(requiredResourceType) => setVisit({ ...visit, requiredResourceType })}><SelectTrigger className="mt-2"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="none">No resource required</SelectItem><SelectItem value="room">Room</SelectItem><SelectItem value="equipment">Equipment</SelectItem><SelectItem value="telehealth_link">Telehealth link</SelectItem></SelectContent></Select></div><DialogFooter><Button className="bg-teal-700 hover:bg-teal-800" disabled={!visit.specialtyIds.length}>Save visit type</Button></DialogFooter></form></DialogContent></Dialog>
  </div></div>{notice && <div className="mt-5 flex items-center gap-2 rounded-md border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-800"><CheckCircle2 className="size-4" />{notice}</div>}{error && <p className="mt-5 rounded-md border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</p>}
  <div className="mt-8 grid gap-6 lg:grid-cols-2"><Card className="bg-white shadow-none"><CardHeader><CardTitle className="flex items-center gap-2"><Stethoscope className="size-5 text-violet-700" />Provider specialties</CardTitle><CardDescription>Assign these to Doctors from the Staff page.</CardDescription></CardHeader><CardContent>{specialties.length ? <div className="flex flex-wrap gap-2">{specialties.map((x) => <span className="rounded-md bg-violet-50 px-3 py-2 text-sm text-violet-800" key={x.id}>{x.name}</span>)}</div> : <p className="text-sm text-muted-foreground">Add specialties before creating visit types or assigning Doctors.</p>}</CardContent></Card><Card className="bg-white shadow-none"><CardHeader><CardTitle className="flex items-center gap-2"><ClipboardList className="size-5 text-teal-700" />Visit types</CardTitle><CardDescription>These set provider eligibility, duration and required resource during booking.</CardDescription></CardHeader><CardContent>{visitTypes.length ? <div className="space-y-3">{visitTypes.map((x) => <div className="rounded-lg border p-3" key={x.id}><p className="font-medium">{x.name} <span className="text-sm font-normal text-muted-foreground">· {x.durationMinutes} min</span></p><p className="mt-1 text-xs text-muted-foreground">{x.specialties.map((s) => s.name).join(", ")} · {x.requiredResourceType ? `Requires ${x.requiredResourceType.replaceAll("_", " ")}` : "No resource required"}</p></div>)}</div> : <p className="text-sm text-muted-foreground">No visit types configured yet.</p>}</CardContent></Card><Card className="bg-white shadow-none"><CardHeader><CardTitle className="flex items-center gap-2"><Boxes className="size-5 text-teal-700" />Branch resources</CardTitle><CardDescription>Resources are selected or required during appointment booking.</CardDescription></CardHeader><CardContent><Dialog open={resourceOpen} onOpenChange={setResourceOpen}><DialogTrigger asChild><Button size="sm" variant="outline"><Plus className="mr-2 size-4" />Add resource</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>Add scheduling resource</DialogTitle></DialogHeader><form className="space-y-4" onSubmit={createResource}><Input value={resource.name} onChange={(e) => setResource({ ...resource, name: e.target.value })} placeholder="e.g. Consultation Room 1" required /><Select value={resource.type} onValueChange={(type) => setResource({ ...resource, type })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="room">Room</SelectItem><SelectItem value="equipment">Equipment</SelectItem><SelectItem value="telehealth_link">Telehealth link</SelectItem></SelectContent></Select><DialogFooter><Button className="bg-teal-700 hover:bg-teal-800">Save resource</Button></DialogFooter></form></DialogContent></Dialog><div className="mt-4 space-y-2">{resources.map((x) => <p className="rounded-lg border p-3 text-sm" key={x._id}>{x.name} <span className="text-muted-foreground">· {x.type.replaceAll("_", " ")}</span></p>)}</div></CardContent></Card><Card className="bg-white shadow-none"><CardHeader><CardTitle className="flex items-center gap-2"><Settings2 className="size-5 text-violet-700" />Scheduling safeguards</CardTitle><CardDescription>Branch policy for overbooking and real SMS appointment reminders.</CardDescription></CardHeader><CardContent><form className="space-y-4" onSubmit={saveSettings}><div><Label>Max overbook slots per doctor per day</Label><Input className="mt-2" type="number" min="0" max="20" value={settings.maxOverbookSlotsPerDoctorPerDay} onChange={(e) => setSettings({ ...settings, maxOverbookSlotsPerDoctorPerDay: e.target.value })} /></div><div><Label>SMS reminder (hours before visit)</Label><Input className="mt-2" type="number" min="0" max="8760" value={settings.reminderHours} onChange={(e) => setSettings({ ...settings, reminderHours: e.target.value })} /><p className="mt-2 text-xs text-muted-foreground">Default: 12 hours. SMS sends only when the patient has not opted out.</p></div><Button className="bg-teal-700 hover:bg-teal-800">Save scheduling rules</Button></form></CardContent></Card></div></>;
}
